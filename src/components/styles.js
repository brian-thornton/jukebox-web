const albumCardStyle = {
  background: 'transparent',
  color: 'white',
  borderColor: '#708090',
  margin: '10px',
  maxWidth: '200px',
};

const albumCardLarge = {
  background: 'transparent',
  color: 'white',
  borderColor: '#708090',
  maxWidth: '400px',
  margin: '10px',
};

const albumImage = {
  maxHeight: '300px', 
  maxWidth: '250px'
};

const albumTitle = {
  maxHeight: '25px', 
  fontSize: '12px', 
  overflow: 'hidden'
};

const cardStyle = {
  background: 'transparent',
  borderColor: '#708090',
  color: 'white'
};

const buttonStyle = {
  margin: '5px'
};

const disabledStyle = {
  background: '#FF0000',
  margin: '5px',
  width: '100px',
}

const enabledStyle = {
  background: '#7CFC00',
  color: '#000000',
  margin: '5px',
  width: '100px',
}

export default {
  albumCardLarge,
  albumCardStyle,
  albumImage,
  albumTitle,
  buttonStyle,
  cardStyle,
  disabledStyle,
  enabledStyle
}